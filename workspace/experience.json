{
    "stable_baseline": "v4.0.0",
    "lessons_learned": [
        {
            "topic": "Architecture",
            "entry": "Machine-to-machine communication must be strict JSON to minimize token bloat."
        },
        {
            "topic": "Dependencies",
            "entry": "Always check for Python version compatibility before installing heavy libraries like yt-dlp or whisper."
        }
    ],
    "owner_rules": [
        "Use local Docker containers for all heavy processing.",
        "Never use cloud databases unless explicitly requested."
    ]
}
