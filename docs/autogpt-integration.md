# AutoGPT Integration

This document tracks the first integration target for the runtime.

The first safe connection layer is a Prometheus-owned bridge that exposes tool status, routes, and task handoff points before any deeper code merge happens.
